version: "3.8"

services:
  agent_backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agent_backend
    restart: unless-stopped
    ports:
      - "8000:8000" # Expose port to Mac host
    env_file:
      - .env